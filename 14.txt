<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para mi Valentín</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@600&family=Quicksand:wght@300;500&display=swap" rel="stylesheet">
    <style>
        :root {
            --color-rosa: #ffafcc;
            --color-rojo: #ff5c8a;
            --color-crema: #fff0f3;
        }

        body {
            margin: 0;
            padding: 0;
            background-color: var(--color-crema);
            font-family: 'Quicksand', sans-serif;
            color: #4a4a4a;
            overflow-x: hidden;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* Fondo Interactivo de Corazones */
        #bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
        }

        /* Contenedor Principal */
        .container {
            width: 90%;
            max-width: 400px;
            padding: 40px 20px;
            text-align: center;
            z-index: 1;
        }

        h1 {
            font-family: 'Dancing Script', cursive;
            font-size: 3rem;
            color: var(--color-rojo);
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }

        /* Estilo de la Carta */
        .carta {
            background: rgba(255, 255, 255, 0.8);
            padding: 25px;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(255, 175, 204, 0.3);
            line-height: 1.6;
            margin-bottom: 30px;
            border: 1px solid white;
        }

        /* Galería de Imágenes */
        .foto {
            width: 100%;
            border-radius: 15px;
            margin: 15px 0;
            border: 5px solid white;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            transform: rotate(-2deg);
            transition: transform 0.3s;
        }

        .foto:nth-child(even) {
            transform: rotate(2deg);
        }

        /* Botón Final */
        .btn-romantico {
            display: inline-block;
            padding: 15px 30px;
            background-color: var(--color-rojo);
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 500;
            box-shadow: 0 5px 15px rgba(255, 92, 138, 0.4);
            transition: transform 0.2s;
            margin-top: 20px;
        }

        .btn-romantico:active {
            transform: scale(0.95);
        }

        /* Animación de corazones flotantes */
        .corazon {
            position: absolute;
            color: var(--color-rosa);
            font-size: 20px;
            animation: flotar linear infinite;
        }

        @keyframes flotar {
            0% { transform: translateY(100vh) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-10vh) rotate(360deg); opacity: 0; }
        }
    </style>
</head>
<body>

    <div id="bg-animation"></div>

    <div class="container">
        <h1>Para mi Persona Favorita</h1>

        <img src="tu-imagen-1.jpg" alt="Nuestro momento" class="foto">

        <div class="carta">
            <p>
                Aquí puedes escribir todo lo que sientes. 
                Este es un espacio dedicado a nosotros, 
                lleno de recuerdos y momentos especiales. 
                Gracias por estar en mi vida. ❤️
            </p>
        </div>

        <img src="tu-imagen-2.jpg" alt="Otro recuerdo" class="foto">

        <a href="https://tu-link-aqui.com" class="btn-romantico">Haz clic aquí, amor 🌹</a>
    </div>

    <script>
        // Generador de corazones de fondo
        function crearCorazon() {
            const container = document.getElementById('bg-animation');
            const corazon = document.createElement('div');
            corazon.classList.add('corazon');
            corazon.innerHTML = '❤';
            corazon.style.left = Math.random() * 100 + 'vw';
            corazon.style.animationDuration = Math.random() * 3 + 2 + 's';
            corazon.style.opacity = Math.random();
            
            container.appendChild(corazon);

            setTimeout(() => {
                corazon.remove();
            }, 5000);
        }

        setInterval(crearCorazon, 300);
    </script>
</body>
</html>